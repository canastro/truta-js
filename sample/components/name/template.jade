template#nameTagTemplate
    style
        include style.css

    .outer
        .boilerplate Hi! My name is
        .name.bindable
            | cenas: ${data.name} ${data.last_name}
        input#name-input
